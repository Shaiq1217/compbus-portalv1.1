import React from 'react';
import './index.css'; // Ensure Spinner.css is correctly imported for styling

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
